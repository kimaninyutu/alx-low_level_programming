#include "holberton.h"

/**
 * _isdigit -  a function that checks for a digit 0-9
 * @c: input
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
0-isupper.c if ((c >= 48) && (c <= 57))
0-isupper.c 0-isupper.c return (1);
0-isupper.c else
0-isupper.c 0-isupper.c return (0);
}
